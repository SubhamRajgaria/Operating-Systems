#include <bits.stdc++.h>

vector <float> arrival_times;
vector < float> bursts;

int main(){
	int n;
	int mean;
	cout << "Enter number of processes " << endl;
	cin >> n;
	cout << "Enter mean " << endl;
	cin >> mean;
	for(int i=0; i< n; i++)
	{
		float time = ((float)rand())/RAND_MAX*5;
		time = 
	} 
}